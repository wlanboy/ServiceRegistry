spring:
  application:
    name: serviceregistry
    
server:
  port: ${PORT:8761}

eureka:
  client:
    registerWithEureka: false
    fetchRegistry: true
    serviceUrl:
      defaultZone: ${EUREKAZONE:http://127.0.0.1:8761/eureka/}    
  server:
      waitTimeInMsWhenSyncEmpty: 0
  instance:
      hostname: ${EUREKAHOST}

logging:
  level:
    com:
      netflix:
        eureka: OFF
        discovery: OFF
              
